# === Large data files ===
*.h5ad
*.h5
*.loom
*.tar.gz

# === Large output directories ===
analysis/
data/
dropbox-data/

# === Figures (working set; only docs/eec_subtypes_v{3,5}.png banner images are tracked) ===
figures/

# === Local archive (history of dead-end + superseded work, kept locally only) ===
archive/

# === Local-only project instructions (CLAUDE.md at any depth) ===
CLAUDE.md
**/CLAUDE.md

# === HPC pipeline (cluster-only, not synced via git) ===
hpc/

# === Notebook 9 — IFN pseudotime, not for clone reproduction ===
notebooks/d10-lapa/9_EEC_interferon_pseudotime.ipynb

# === Loose exploratory plots in d10-lapa/ (threshold tuning, EEC variants) ===
notebooks/d10-lapa/*.png
notebooks/d10-lapa/*.pdf

# === Claude analysis outputs (large scored files) ===
claude/decoupler/
claude/enrichment/
claude/enrichment_gseapy/

# === Separate repo (consolidated into notebooks/dge/) ===
more-egfr-singlecell/

# === Anaconda project databases ===
anaconda_projects/db/
notebooks/anaconda_projects/

# === Python ===
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/

# === Jupyter ===
.ipynb_checkpoints/

# === macOS ===
.DS_Store
*DS_Store

# === IDE ===
.idea/
.vscode/
*.swp
*.swo
